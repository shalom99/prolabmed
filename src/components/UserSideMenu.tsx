import { FC } from 'react'

type UserSideMenuProps = {
  
}

const UserSideMenu: FC<UserSideMenuProps> = ({}) => {
  return (
     <div>
        <nav>
            
        </nav>
     </div>
)
}

export default UserSideMenu